# راهنمای ورود CSV/XLSX، کیفیت داده و تحلیل تصمیم‌گیری QueueCraft

## ۱. جریان ورود داده و کنترل کیفیت

QueueCraft دو نوع داده را می‌پذیرد. در دادهٔ **رویدادی**، هر ردیف یک ورود است و تنها به یک ستون زمان نیاز دارد؛ سیستم برای هر ردیف معتبر یک ورود ثبت می‌کند. در دادهٔ **تجمیع‌شده**، علاوه بر ستون زمان یک ستون تعداد ورود وجود دارد و مقدار آن در بازه زمانی انتخاب‌شده جمع می‌شود. این طراحی اجازه می‌دهد داده‌های خام نوبت‌دهی و همچنین گزارش‌های ساعتی یا روزانه وارد مدل شوند.

| مرحله | رفتار پیاده‌سازی‌شده | نتیجه قابل بررسی |
|---|---|---|
| محدودیت منبع | پذیرش فقط CSV/XLS/XLSX، سقف پیش‌فرض ۲۵MB و یک میلیون ردیف | جلوگیری از ورود فرمت یا حجم ناامن |
| تشخیص شِما | تشخیص نام‌های مرسوم زمان و تعداد ورود؛ امکان تعیین صریح نام ستون | کاهش خطای نگاشت دستی ستون‌ها |
| پاک‌سازی شفاف | حذف زمان نامعتبر، تعداد غیرعددی و تعداد منفی | شمار هر دسته خطا در گزارش کیفیت ثبت می‌شود |
| تجمیع زمانی | تبدیل زمان به bucket ساعتی پیش‌فرض و پرکردن بازه‌های گمشده با صفر | سری زمانی کامل برای پیش‌بینی و مونت‌کارلو |
| کیفیت و ممیزی | کیفیت درصدی، هشدارها، پیش‌نمایش و محدوده زمانی | هیچ حذف یا پرکردنی بدون ثبت در گزارش انجام نمی‌شود |

### شِمای پیشنهادی فایل

```csv
timestamp,arrivals
2026-01-05 08:00:00,12
2026-01-05 09:00:00,16
2026-01-05 10:00:00,20
```

برای داده رویدادی، ستون `arrival_time` کافی است و ستون‌های دیگر مانند `customer_id` نادیده گرفته می‌شوند:

```csv
arrival_time,customer_id
2026-01-05 08:10:00,C-1001
2026-01-05 08:14:00,C-1002
```

در رابط دسکتاپ روی **Import CSV/XLSX** کلیک کنید، فایل را از انتخاب‌گر بومی برگزینید و کیفیت داده را پیش از اجرای تحلیل بررسی کنید. داده واردشده فقط در حافظه جلسه برای تحلیل‌های AI–Monte Carlo، بهینه‌سازی ظرفیت، پارتو و حساسیت استفاده می‌شود. برای نگهداری قابل ممیزی، سناریو را از دکمه **Save Scenario** ذخیره کنید.

### استفاده برنامه‌نویسی

```python
from data_import import ImportOptions, import_arrival_data
from ai_monte_carlo import run_ai_monte_carlo

imported = import_arrival_data(
    "examples/arrival_history_sample.csv",
    ImportOptions(timestamp_column="timestamp", count_column="arrivals", frequency="h"),
)
print(imported.quality.quality_score_pct)

result = run_ai_monte_carlo(
    imported.historical_counts,
    [{"name": "Service", "servers": 3, "mean_service_time": 0.4, "service_cv": 0.9}],
    replications=500,
    seed=42,
)
```

> **اصل کنترل کیفیت:** درصد کیفیت بالا به معنی مناسب‌بودن مدل کسب‌وکار نیست؛ فقط نشان می‌دهد چه سهمی از ردیف‌های فایل با قواعد ورودی سازگار بوده‌اند. پیش از تصمیم عملیاتی، بازه زمانی، تعریف bucket، معنی ستون‌ها و نمایندگی داده نسبت به عملیات واقعی را تأیید کنید.

## ۲. تحلیل حساسیت

تحلیل حساسیت مشخص می‌کند اگر دو عدم‌قطعیت اصلی تغییر کنند، خروجی صف چه‌قدر آسیب‌پذیر است. QueueCraft در هر سناریو، ضریب تقاضا و ضریب زمان خدمت را تغییر می‌دهد و برای هر ترکیب Monte Carlo را با همان seed اجرا می‌کند. نگه‌داشتن seed مشترک، مقایسه را منصفانه‌تر می‌کند؛ تفاوت نتایج از فرض سناریو ناشی می‌شود، نه از نمونه تصادفی متفاوت.

به صورت پیش‌فرض، ماتریس ۳×۳ با ضرایب ۸۰٪، ۱۰۰٪ و ۱۲۰٪ برای تقاضا و زمان خدمت ساخته می‌شود. خروجی شامل میانگین انتظار، P95 انتظار، تعداد کارهای مورد انتظار و Makespan است. در رابط، دو نمودار خطی میانگین انتظار و P95 را نمایش می‌دهند؛ خروجی PNG heatmap نیز برای گزارش‌های مدیریتی قابل تولید است.

```python
from decision_analytics import sensitivity_analysis, render_sensitivity_chart

analysis = sensitivity_analysis(history, tiers, replications=100, seed=42)
render_sensitivity_chart(analysis, "sensitivity.png")
```

## ۳. تحلیل پارتو برای هزینه صف

تحلیل پارتو با دو هدف هم‌زمان کار می‌کند: **کمینه‌کردن هزینه سرورها** و **کمینه‌کردن زمان انتظار میانگین انتها به انتها**. هر ترکیب تعداد سرورها در مراحل صف اجرا می‌شود. یک ترکیب زمانی «مغلوب» است که ترکیب دیگری هم هزینه کمتر یا مساوی و هم زمان انتظار کمتر یا مساوی داشته باشد و در دست‌کم یکی از این دو بهتر باشد. فقط ترکیب‌های غیرمغلوب روی جبهه پارتو باقی می‌مانند.

| وضعیت SLA | منطق پیشنهاد |
|---|---|
| یک یا چند طرح پارتو SLA را رعایت می‌کنند | کم‌هزینه‌ترین طرح پارتوِ منطبق با SLA انتخاب می‌شود |
| هیچ طرحی SLA را رعایت نمی‌کند | نقطه متعادل نزدیک به حالت ایده‌آل هزینه/انتظار پیشنهاد می‌شود و دلیل پیشنهاد صریحاً اعلام می‌گردد |
| SLA تعریف نشده است | نقطه متعادل جبهه پارتو پیشنهاد می‌شود، اما تمام گزینه‌های غیرمغلوب باقی می‌مانند |

```python
from decision_analytics import capacity_pareto_analysis, render_pareto_chart

analysis = capacity_pareto_analysis(
    history, tiers,
    server_range=(1, 6),
    cost_per_server=1.0,
    sla_mean_wait=5.0,
    replications=100,
    seed=42,
)
render_pareto_chart(analysis, "pareto.png")
```

نمودار پارتو، همه نامزدها را با خاکستری، جبهه غیرمغلوب را با آبی و طرح پیشنهادی را با نشان طلایی نمایش می‌دهد. نمودار در `assets/analytics/pareto_tradeoff_illustrative.png` صرفاً یک سناریوی نمونه داخلی است و نباید به‌عنوان داده یا نتیجه عملیاتی مشتری تفسیر شود.

## ۴. قابلیت‌های تجاری پیشنهادی بعدی

نسخه بعدی باید «آمادگی داده» را با **Data Dictionary سفارشی، اعتبارسنجی قواعد دامنه‌ای، نگاشت ستون‌های ذخیره‌شده، تشخیص داده پرت و گزارش MAPE/MAE خارج از نمونه** تقویت کند. برای تحلیل تصمیم نیز، تخصیص هزینه متغیر بر اساس ساعت، جریمه نقض SLA، تحلیل جبهه سه‌هدفه شامل بهره‌برداری و قابلیت مقایسه نسخه‌های سناریو، ارزش تجاری بالایی دارند.
