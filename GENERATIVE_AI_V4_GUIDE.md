# راهنمای پیشنهاددهنده مولد کنترل‌شده QueueCraft Sim v4.0

**نسخه:** 4.0 Preview
**دامنه:** پیشنهاددهی ظرفیت و پیکربندی صف؛ بدون اعمال تغییر در سامانه‌های عملیاتی

## هدف و مرز عملیاتی

ماژول [`generative_queue_optimizer.py`](./generative_queue_optimizer.py) یک لایه تصمیم‌یار روی خروجی‌های از پیش محاسبه‌شده تحلیل پارتو و تحلیل حساسیت است. این ماژول برای انتخاب یا توضیح یک گزینه از **کاتالوگ تأییدشده نامزدهای شبیه‌سازی‌شده** طراحی شده است، نه برای اجرای تغییرات ظرفیت، تماس با سامانه‌های ابری، یا تولید یک طرح خارج از نتایج مدل.

> پیشنهاد v4.0 یک **پیش‌نویس قابل ممیزی** است. نتیجه آن هرگز به‌صورت خودکار اعمال نمی‌شود و همواره نیازمند بازبینی، بازاجرای شبیه‌سازی و تأیید صریح اپراتور است.

| جزء | مسئولیت | حد مجاز |
|---|---|---|
| تحلیل پارتو | تولید گزینه‌های ظرفیت، هزینه، انتظار و انطباق SLA | تنها منبع مجاز انتخاب ظرفیت |
| تحلیل حساسیت | نشان‌دادن اثر تغییر تقاضا و زمان خدمت | زمینه ریسک برای پیشنهاد |
| پیشنهاددهنده قطعی آفلاین | انتخاب کم‌هزینه‌ترین نامزد واجد محدودیت | حالت پیش‌فرض رابط دسکتاپ |
| مدل مولد اختیاری | نگارش توضیح ساخت‌یافته و انتخاب شناسه نامزد | فقط با `enable_llm=True` و کلید محیطی |
| اپراتور انسانی | تأیید هر تغییر واقعی | تنها مجری مجاز تغییرات عملیاتی |

## کنترل‌های ایمنی پیاده‌سازی‌شده

کاتالوگ ورودی شامل شناسه، تعداد سرورهای هر لایه، هزینه، میانگین و صدک ۹۵ زمان انتظار، بهره‌برداری و وضعیت SLA است. شناسه‌اثر `SHA-256` از بسته شواهد ساخته می‌شود تا هر پیش‌نویس به داده‌های ارزیابی‌شده متناظر خود قابل ردیابی باشد. خروجی مدل مولد تنها مجاز است `candidate_id` را از همان کاتالوگ انتخاب کند؛ شناسه نامعتبر باعث توقف با خطا می‌شود.

| کنترل | رفتار تضمین‌شده |
|---|---|
| حالت پیش‌فرض | `enable_llm=False` و پیشنهاد قطعی کاملاً محلی |
| محدودیت‌پذیری | سقف انتظار، سقف هزینه و الزام انطباق SLA پیش از انتخاب اعمال می‌شوند |
| عدم‌اعمال | `applied` همیشه `False` است |
| عدم عملیات بیرونی | `external_operations_performed` همیشه `False` است |
| تأیید انسانی | `approval_required` همیشه `True` است |
| حداقل‌سازی داده | بسته شواهد شامل خروجی تحلیل است، نه رکوردهای خام مشتری |
| خروجی ساخت‌یافته | پاسخ مدل باید با JSON Schema محدود مطابقت کند |
| نبود کلید | استفاده صریح از مدل در نبود `OPENAI_API_KEY` با خطای روشن متوقف می‌شود |

## اجرای آفلاین از رابط دسکتاپ

در نسخه دسکتاپ، دکمه **Create v4.0 AI Queue Draft (Offline)** یک تحلیل پارتو و حساسیت محلی اجرا می‌کند و سپس پیش‌نویس قطعی می‌سازد. این دکمه عمداً `enable_llm: false` می‌فرستد. نتیجه در پنل تحلیل AI نمایش داده می‌شود و شامل تعداد سرورهای پیشنهادی، هزینه مدل‌شده، زمان انتظار و هشدار «نیازمند بازبینی» است.

این مسیر با متد `create_v4_queue_proposal` در [`app.py`](./app.py) متصل است. متد مزبور هیچ تغییری در زیرساخت یا ذخیره‌سازی بیرونی ایجاد نمی‌کند.

## استفاده برنامه‌نویسی‌شده

نمونه زیر پیش‌نویس را صرفاً از خروجی‌های محاسبه‌شده تولید می‌کند. پارامتر `pareto_analysis` باید ساختار خروجی `capacity_pareto_analysis` را داشته باشد و `sensitivity_analysis` اختیاری است.

```python
from generative_queue_optimizer import create_generative_proposal

proposal = create_generative_proposal(
    pareto_analysis=pareto_result,
    sensitivity_analysis=sensitivity_result,
    constraints={
        "require_sla_compliance": True,
        "max_mean_wait": 5.0,
        "max_server_cost": 12.0,
    },
    enable_llm=False,
)

assert proposal["approval_required"] is True
assert proposal["applied"] is False
assert proposal["external_operations_performed"] is False
```

در صورت فعال‌سازی آگاهانه مدل مولد، بسته به تنظیمات محیط اجرا، کلید `OPENAI_API_KEY` باید در محیط امن فرآیند تعریف شود؛ کلید هرگز نباید در کد، فایل تنظیمات نسخه‌بندی‌شده، گزارش یا نصاب Windows ثبت شود. فعال‌سازی باید فقط توسط اپراتور مجاز و پس از ارزیابی سیاست حریم خصوصی سازمان انجام شود.

## گردش تأیید پیشنهادی

| گام | مالک | خروجی قابل ممیزی |
|---|---|---|
| ۱. ورود و کنترل کیفیت داده | تحلیلگر | گزارش کیفیت و دامنه داده |
| ۲. اجرای مونت‌کارلو | تحلیلگر | seed، تعداد تکرار و خروجی شبیه‌سازی |
| ۳. تحلیل پارتو و حساسیت | تحلیلگر | کاتالوگ نامزدها و سناریوهای ریسک |
| ۴. ساخت پیش‌نویس v4.0 | تصمیم‌یار | fingerprint شواهد و پیشنهاد محدود |
| ۵. بازبینی مستقل | مالک سرویس | تأیید SLA، بودجه و ریسک |
| ۶. اجرای بیرونی (در صورت نیاز) | تیم عملیات | تغییر دستی و ثبت‌شده خارج از QueueCraft |
| ۷. اعتبارسنجی پس از تغییر | مالک سرویس | مقایسه پیش‌بینی و مشاهده واقعی |

## آزمون و دروازه انتشار

آزمون [`test_generative_queue_optimizer.py`](./test_generative_queue_optimizer.py) انتخاب قطعی، فیلترشدن محدودیت‌ها، fingerprint شواهد، رد شناسه خارج از کاتالوگ و نبود عملیات بیرونی را پوشش می‌دهد. این آزمون همراه با سناریوهای استرس توزیع‌شده در `npm run test:ai`، اسکریپت `build_windows.ps1` و گردش کار ساخت Windows اجرا می‌شود.

برای اجرای مجموعه کامل آزمون‌ها:

```bash
npm test
npm run test:ai
```

## توسعه‌های تجاری پیشنهادی بعدی

| اولویت | قابلیت | ارزش تجاری | شرط ایمنی پیش از عرضه |
|---|---|---|---|
| P1 | نسخه‌بندی سیاست و جریان تأیید چندمرحله‌ای | حاکمیت تصمیم و انطباق‌پذیری سازمانی | نقش‌ها، امضای تأیید و لاگ تغییرناپذیر محلی |
| P1 | کتابخانه سناریوی سازمانی با مقایسه نسخه‌ها | تکرارپذیری و همکاری تیمی | fingerprint، کنترل دسترسی و خروجی فقط‌خواندنی |
| P2 | تشخیص ناهنجاری تقاضا با هشدار محلی | واکنش سریع به جهش‌های غیرعادی | عدم ارسال تله‌متری و آستانه‌های قابل بازبینی |
| P2 | تحلیل هزینه/کربن ظرفیت و نمودار تصمیم | حمایت از FinOps و اهداف پایداری | منابع هزینه و ضریب انتشار کاملاً قابل استناد |
| P3 | اتصال اختیاری به Runbook سازمانی | کوتاه‌کردن چرخه تصمیم | اتصال فقط‌خواندنی، تأیید انسانی و ثبت حسابرسی |
| P3 | ارزیابی پس از اجرا و کالیبراسیون مدل | بهبود دقت پیش‌بینی در طول زمان | داده ناشناس‌سازی‌شده و چرخه تأیید تغییر مدل |

## جمع‌بندی

v4.0 Preview ظرفیت تصمیم‌یار QueueCraft را توسعه می‌دهد، اما اختیار عملیاتی را حفظ می‌کند. این طراحی، مدل مولد را به **توضیح‌دهنده محدودشده توسط شواهد** تبدیل می‌کند: قابلیت انتخاب آن به نتایج محاسبه‌شده محدود است، خروجی آن قابل ردیابی است و هیچ مسیر خودکار برای تغییر ظرفیت وجود ندارد.
