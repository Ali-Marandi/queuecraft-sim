# راهنمای اجرای تست استرس توزیع‌شده در QueueCraft

## محدوده و اصل ایمنی

تست‌های این راهنما از ماژول `distributed_load_testing.py` استفاده می‌کنند و فقط یک **شبیه‌سازی محلی ظرفیت** هستند. هیچ درخواست شبکه، URL هدف، credential یا traffic واقعی ایجاد نمی‌شود. عبارت «تست استرس» در این نسخه به معنای اجرای تکراری سناریوهای ترافیک سنگین روی مدل QueueCraft است؛ نه ارسال بار به یک سرویس بیرونی.

## اجرای سریع

```bash
cd /path/to/queuecraft-sim
python3 distributed_stress_scenarios.py --scenario all
```

برای اجرای یک سناریوی مشخص و ذخیره گزارش JSON:

```bash
python3 distributed_stress_scenarios.py \
  --scenario regional_outage_peak \
  --output ./artifacts/regional_outage_peak.json
```

خروجی هر سناریو شامل درخواست مدل‌شده، درخواست تولیدشده، محدودیت مولد، درخواست بدون سرویس، درصد سرویس‌دهی، P95 latency مدل‌شده و وضعیت قبولی معیارهای سناریو است.

## سناریوهای آماده

| شناسه | الگوی فشار | هدف بررسی | معیار پذیرش نمونه |
|---|---|---|---|
| `global_peak` | افزایش شدید تقاضا در همه مناطق بدون outage | نقطه اشباع ظرفیت کل | سرویس‌دهی ≥ ۹۹٪ و P95 زیر ۲۵۰ms |
| `regional_outage_peak` | پیک جهانی همراه با از دست‌رفتن Europe region در قله | ظرفیت failover و latency بین‌قاره‌ای | سرویس‌دهی ≥ ۹۰٪، unserved زیر سقف و P95 زیر ۴۰۰ms |
| `generator_constrained` | تقاضای بیشتر از سقف agentهای مدل‌شده | تمایز محدودیت تولید بار از محدودیت سرویس | گزارش صریح generator-limited و عدم شکست اعتبار داده |
| `sustained_saturation` | بار نزدیک/بالاتر از ظرفیت در چند bucket متوالی | فشار پایدار و هزینه saturation | تشخیص peak unserved و برچسب عدم‌قبول در صورت عبور از SLA |
| `latency_partition` | افزایش latency مسیرهای یک قاره بدون outage کامل | حساسیت routing به network degradation | گزارش افت SLO latency حتی در نبود unserved |

مقادیر معیارها باید پیش از استفاده سازمانی با SLA، RPO/RTO، الگوی واقعی تقاضا و هزینه کسب‌وکار جایگزین شوند. مقادیر پیش‌فرض در این فایل فقط گاردریل سناریوی شبیه‌سازی هستند.

## نحوه تفسیر نتیجه

| شاخص | معنی عملیاتی | اقدام پیشنهادی در صورت شکست |
|---|---|---|
| `generator_capacity_limited_requests` | agentهای مدل‌شده نتوانستند همه تقاضا را تولید کنند | سقف مولد را از ظرفیت سرویس جداگانه بررسی کنید |
| `total_unserved_requests` | ظرفیت regionهای سالم برای بار تولیدشده کافی نبوده است | ظرفیت، routing یا سیاست Failover را بازنگری کنید |
| `global_p95_estimated_latency_ms` | tail latency مدل‌شده روی درخواست‌های سرویس‌داده‌شده | locality، ظرفیت جایگزین و آستانه SLO را بررسی کنید |
| `served_pct_of_generated` | درصد خدمت نسبت به بار تولیدشده | با SLO availability مورد توافق مقایسه کنید |
| `acceptance.passed` | همه معیارهای محلی سناریو برقرارند | نتیجه صرفاً گواه عبور از مدل است، نه تأیید محیط تولید |

## ترکیب با جریان انتشار

اجرای سریع همه سناریوها می‌تواند به gate کیفی CI اضافه شود، اما معیارهای هزینه‌بر یا سناریوهای بسیار بزرگ باید به یک job دستی یا زمان‌بندی‌شده جدا منتقل شوند. تحلیل محلی QueueCraft تعیین نمی‌کند که traffic واقعی به یک سامانه بیرونی فرستاده شود؛ تست بیرونی نیازمند مالکیت هدف، سقف نرخ، kill switch، پنجره تغییر و تأیید صریح است.
