# معماری Multi-Region Failover و SLO در QueueCraft

## محدوده

این قابلیت یک **شبیه‌ساز محلی تصمیم‌یار** است. ورودی آن، بار bucket‌شده، مشخصات مناطق، سیاست routing و رخدادهای خرابی از پیش‌تعریف‌شده است. خروجی، نحوه تخصیص بار، ظرفیت مصرف‌شده، بار پاسخ‌نداده، latency مدل‌شده، رویدادهای failover و وضعیت SLO است. این ماژول هیچ API ابری، credential یا تغییر واقعی زیرساخت را فراخوانی نمی‌کند.

## مدل منطقه

هر Region دارای نام، ظرفیت مجاز در هر bucket، وزن routing، latency پایه و وضعیت سلامت است. وضعیت سلامت می‌تواند با رخداد outage در هر bucket تغییر کند. Region ناسالم هیچ بار جدیدی دریافت نمی‌کند. Region سالم ابتدا بار ترجیحی خود را می‌گیرد و سپس در صورت فعال بودن failover، بار باقی‌مانده به ظرفیت آزاد مناطق سالم دیگر منتقل می‌شود.

| مؤلفه | تصمیم طراحی |
|---|---|
| Routing عادی | `weighted_capacity` بر اساس وزن و ظرفیت آزاد |
| Failover | انتقال فقط به مناطق سالم دارای ظرفیت آزاد |
| Active-passive | ابتدا primary، سپس secondaryهای سالم |
| Active-active | توزیع وزنی میان مناطق سالم و بازتوزیع هنگام outage |
| داده از دست‌رفته | `unserved_jobs` به‌صورت صریح ثبت می‌شود؛ هیچ موفقیت کاذب ایجاد نمی‌شود |
| Latency | میانگین وزنی latency مسیرهای سرویس‌داده‌شده با جریمه failover محاسبه می‌شود |

## SLO و Error Budget

SLO در این مدل با دو شرط تعریف می‌شود: نسبت درخواست‌های سرویس‌داده‌شده و latency مدل‌شده زیر آستانه. بودجه خطا برابر است با تعداد مجاز درخواست‌های ناموفق در پنجره، یعنی:

```text
allowed_bad = total_requests × (1 − availability_target)
remaining_budget = allowed_bad − observed_bad
```

علاوه بر موجودی بودجه، burn rate کوتاه‌مدت و بلندمدت بررسی می‌شود تا هشدار زودهنگام ممکن شود. `SLOMonitor` فقط observationهای محلی را می‌پذیرد و snapshot قابل ارسال به هر telemetry backend را تولید می‌کند.

## گاردریل‌ها

1. حداقل یک region باید وجود داشته باشد و ظرفیت و وزن معتبر داشته باشد.
2. رخداد outage فقط وضعیت شبیه‌سازی را تغییر می‌دهد، نه زیرساخت واقعی را.
3. Failover تنها بار قابل سرویس در ظرفیت آزاد را منتقل می‌کند.
4. SLO output باید شامل تعریف پنجره، کل درخواست، bad request، بودجه باقی‌مانده و سطح هشدار باشد.
5. هر تصمیم یا هشدار در timeline ثبت می‌شود تا قابل بازبینی باشد.
