# راهنمای عرضه سازمانی و جهانی‌سازی QueueCraft Enterprise AI

## وضعیت این ارتقا

این ارتقا QueueCraft را از یک ابزار شبیه‌سازی پایه به یک هسته قابل‌عرضه برای تحلیل ظرفیت سازمانی نزدیک‌تر می‌کند. قابلیت‌های مدل‌سازی جدید، مدیریت سناریو و چندزبانه‌سازی به شکلی طراحی شده‌اند که داده‌های عملیاتی به‌صورت پیش‌فرض روی دستگاه کاربر باقی بماند.

| محور | قابلیت پیاده‌سازی‌شده | نتیجه عملی |
|---|---|---|
| واقع‌گرایی عملیاتی | اولویت، ظرفیت صف، رد درخواست و ترک صف | امکان مدل‌سازی تریاژ، مشتری ممتاز، صف‌های محدود و از دست‌رفتن تقاضا |
| حاکمیت مدل | سناریوی JSON اعتبارسنجی‌شده، seed تکرارپذیر و fingerprint SHA-256 | تحلیل قابل بازبینی و بازتولیدپذیر برای تیم عملیات و ممیزی |
| کنترل سطح خدمت | ارزیابی صریح SLA با وضعیت pass/fail و فاصله تا آستانه | جلوگیری از نتیجه‌گیری مبهم از میانگین زمان انتظار |
| جهانی‌سازی | فرهنگ واژگان English/Persian، جهت RTL و انتخاب زبان پایدار | آماده‌سازی محصول برای زبان‌های بعدی بدون بازنویسی منطق |
| عرضه آفلاین | درج localeها در بسته ویندوز و دارایی‌های UI محلی | امکان اجرای رابط بدون CDN پس از ساخت نسخه نصبی |

## قابلیت‌های پیشرفته صف

در تابع `simulate_priority_queue`، مقدار کوچک‌تر در فیلد `priority` به معنای اولویت بالاتر است. این مدل **non-preemptive** است: خدمت جاری قطع نمی‌شود، اما هنگام آزادشدن سرور، مهم‌ترین درخواست منتظر انتخاب می‌شود. این رفتار برای بسیاری از محیط‌های عملیاتی از جمله مراکز تماس، پذیرش و تریاژ مناسب‌تر از قطع مداوم خدمت است.

وقتی `queue_capacity` مشخص باشد، هر ورود جدیدی که همه سرورها مشغول و ظرفیت انتظار پر باشد با وضعیت `rejected` ثبت می‌شود. زمانی که `patience` برای یک درخواست یا `default_patience` برای سیاست صف تعیین شود، درخواست پس از عبور از مهلت انتظار با وضعیت `abandoned` ثبت می‌گردد. خروجی شامل نرخ خدمت، میانگین و P95 انتظار کاربران خدمت‌گرفته، و آمار هر `class_name` است.

## مدیریت سناریوی سازمانی

`ScenarioRepository` به صورت پیش‌فرض سناریوها را در `~/.queuecraft/scenarios` ذخیره می‌کند. هر سند پیش از ذخیره اعتبارسنجی می‌شود و اثرانگشت SHA-256 آن با محتوا تطبیق داده می‌شود. در نتیجه، تغییر دستی و تأییدنشده یک سناریو در زمان بارگذاری تشخیص داده خواهد شد.

ذخیره‌سازی فعلی **محلی** است و برای استقرار تک‌کاربره یا محیط‌هایی که انتقال داده به فضای ابری مجاز نیست مناسب است. پیش از تبدیل آن به مخزن ابری، این حداقل کنترل‌ها باید اضافه شوند: مدیریت هویت، نقش‌های دسترسی، رمزنگاری در حال انتقال و در حالت سکون، خط‌مشی نگهداری داده و ثبت رخدادهای ممیزی.

## افزودن زبان جدید

برای افزودن زبان جدید، یک فرهنگ واژگان تازه در `locales/i18n.js` با کلیدهای فعلی اضافه کنید. برای زبان‌های راست‌به‌چپ، تابع `applyLanguage` به طور خودکار `dir="rtl"` را تنظیم می‌کند. هر متن جدید در UI باید از ابتدا با ویژگی `data-i18n` و یک کلید پایدار تعریف شود تا متن در کد برنامه پراکنده نشود.

## پیشنهادهای آماده برای توسعه بعدی

| قابلیت | ارزش تجاری | شرط ایمن برای پیاده‌سازی |
|---|---|---|
| CSV/XLSX Import Wizard با تشخیص کیفیت داده | کاهش زمان آماده‌سازی سناریو | اعتبارسنجی شِما، گزارش داده گم‌شده و پذیرش صریح کاربر قبل از استفاده |
| شبکه‌های صف با مسیرهای احتمالی | ورود به بازار لجستیک، فرودگاه و تولید | تعریف شفاف احتمال مسیر و امکان بازتولید seed |
| تحلیل حساسیت و جبهه پارتو | مذاکره دقیق میان هزینه و SLA | عدم پنهان‌سازی گزینه‌های مغلوب و ثبت پارامترهای مقایسه |
| پایش خطای پیش‌بینی و drift | جلوگیری از تکیه بر مدل قدیمی | گزارش MAE/MAPE خارج از نمونه و هشدار قابل توضیح |
| همکاری سازمانی با RBAC | فروش به تیم‌ها و سازمان‌های چندسایتی | احراز هویت، مجوز نقش‌محور و ثبت کامل عملیات |
| دستیار زبانی تحلیل‌گر | افزایش دسترس‌پذیری تحلیل برای مدیران غیر فنی | پاسخ فقط بر مبنای داده مجاز، نمایش فرض‌ها و تأیید انسانی برای اقدام |

> اصل عرضه جهانی: هر توصیه هوشمند باید منبع داده، فرض‌ها، سناریوی اجرا، seed و سطح ریسک قابل مشاهده داشته باشد.
